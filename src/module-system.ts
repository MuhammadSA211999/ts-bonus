export const add = (param1: number, param2: number): number => {
    return param1 + param2
}
export const substr = (string1: string, string2: string): string => {
    return string1.concat(string2)
}
const result: number = add(2, 4)